---
color: yellow
priority: low
---

# Implement Sprite Animation

Add the `Animation` struct so that sprites may be animated.

# Details

- Add the `Animation` struct
- Implement the various "Frames" accessors in the locators
- Implement the `Update` and `Draw` methods on the `Animation` struct

