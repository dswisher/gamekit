---
color: yellow
priority: low
---

# Implement Sprite Animation

Add the `Animation` struct so that sprites may be animated.

# Subtasks

- [X] Add the `Animation` struct
    - [X] Constructor, with FPS, loop setting, etc
    - [X] Ability to add frames from images
- [X] Implement the `Update` and `Draw` methods on the `Animation` struct
- [ ] Implement the various "Frames" accessors in the locators and use those to create animations
- [ ] Implement an `AnimationCollection` struct to group together related animations: `SetAnimation("run")`, `SetAnimation("idle")`, etc.

